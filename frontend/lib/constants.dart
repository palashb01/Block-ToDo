import 'package:flutter/material.dart';

const kWhite = Colors.white;
const kBlack = Colors.black;
const kPrimary = Color.fromARGB(191, 0, 0, 0);
const kSecondary = Color.fromARGB(127, 0, 0, 0);
const kCoins = Color.fromARGB(255, 250, 212, 97);
const kShadow = Color.fromARGB(64, 0, 0, 0);